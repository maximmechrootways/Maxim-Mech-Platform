# Environment variables for Maxim Frontend (Vite)
# Only variables prefixed with VITE_ are exposed to the client

# The URL of your deployed backend API (e.g. https://maxim-backend.onrender.com)
VITE_API_URL="http://localhost:3000"
